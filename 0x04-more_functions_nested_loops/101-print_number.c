#include "main.h"

/**
 * print_number - prints an interger
 * @n: integer to be printed
 */
void print_number(int n)

	unsigned int nl;

	if (n < 0)
	{
		nl = -n;
		_putchar('-')
	} else
	{
		n1 = n;
	}

	if (n1 / 10)
	{	
		primtf_number(n1 / 10);
	}

	_putchar(n1 % 10) + '0');
}	

