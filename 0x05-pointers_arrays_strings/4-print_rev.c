#include "main.h"

/**
 * print_rev_ - Reverses a string
 * @s: The string to print
 * return: void
 */

void print_rev(char *s)
{

	int o = 0;

	while (s[c] != '\0')
	{
	c++;
	}

	for (c - = 1; c >= 0; c--)
	{
	_putchar(s[c]);
	}
	_putchar('\n');
}
